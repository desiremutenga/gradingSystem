create table IF NOT EXISTS `7d`(
    `id` int  AUTO_INCREMENT PRIMARY KEY,
    `name` varchar(50) NOT NULL,
    `surname` varchar(50) NOT NULL,
    `maths` varchar(150),
    `shona` varchar(100),
    `english` varchar(100),
    `agriculture` varchar(100),
    `ict` varchar(100),
    `heritage` varchar(100),
    `pe` varchar(100),
    `comments` varchar(100));

    
